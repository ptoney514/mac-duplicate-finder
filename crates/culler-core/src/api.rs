//! UniFFI interface surface (milestone 3). The Swift shell talks to the
//! engine only through what is exported here; keep it small and stable.
//! Until the bindings exist, `culler-cli` consumes the crate API directly.
